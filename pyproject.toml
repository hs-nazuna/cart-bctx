[build-system]
requires = [
    "setuptools>=61.0",
    "wheel",
    "Cython>=3.0",
    "numpy>=2.0",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "pandas",
    "graphviz",
    "sphinx>=7.0"
]
build-backend = "setuptools.build_meta"